using UnityEngine;

public sealed class ShareKit : MonoBehaviour
{
    public void ShareToPlatform(string message, Texture2D image)
    {
        // Placeholder: platform-specific share implementation
        Debug.Log($"Sharing: {message}");
    }
}